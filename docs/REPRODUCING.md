# Reproduce the current release checks

Follow [the complete command list](current-evidence/REPRODUCING.md). In a clone, change directory to `docs/current-evidence` first; the downloaded evidence ZIP runs from its extraction root. All six mandatory arithmetic/integrity checks use the Python standard library. The optional prescribed-score selection demonstration requires PyTorch. No advertised check recreates historical games or training.
