# MEASURED — 2026-08-06

**Every number here was measured or independently re-derived on this date, with its source,
its n, and its interval. Nothing in this file is an estimate carried forward from a document.**

Companion to [`RETRACTIONS.md`](RETRACTIONS.md), which lists what these numbers replace.
Verify with `python scripts/onboard_check.py`.

---

## 1. The ladder rating system — CLOSED

**The ladder is stock TrueSkill 1v1.** Three independent derivations from
`D:/meta-kaggle/episode_agents_116727.csv` (935,009,030 bytes, 9,483,374 rows, on disk since
2026-07-26):

| derivation | beta | p05–p95 | n episodes |
|---|---|---|---|
| investigation agent | 149.810 | — | — |
| apprentice (PR #2693) | 149.812 | 149.65–149.88 | 1,376,672 |
| Master, independent solver, different slice | **149.789** | 149.59–149.86 | 198,757 |

```
mu_0 = 600 · sigma_0 = 200 · tau = 0 · sigma floor 35.0 · max d-sigma 15.0/episode
sigma ladder 200 -> 185 -> 170 -> 155 -> 140 (visible in the raw file)
DISPLAYED LEADERBOARD SCORE = mu EXACTLY (not mu - 3*sigma). Verified vs our ladder_log.csv.
```

### The conversion that governs the campaign

```
c = sqrt(2*beta^2 + sigma_1^2 + sigma_2^2) = 217.55   at the sigma floor of 35
P(win vs an equal-rated peer) = Phi(d_mu / c)
```

| required gap | TrueSkill (actual) | retired Elo-400 estimate |
|---|---|---|
| +82.7 mu (L=836.8) | **+14.81pp** | +11.7pp |
| +100.9 mu (L=855) | **+17.86pp** | +14.1pp |
| +134.9 mu (L=889) | **+23.24pp** | +18.5pp |

Inverse, for sizing: `P=0.60 -> +55 mu · P=0.633 -> +74 mu · P=0.65 -> +84 mu`.

**The Elo-400 conversion is RETIRED.** The target is ~25% harder than it implied.

---

## 2. The screening instrument — MEASURED

`sigma_delta` from 120,000 engine games, true mirror (`rules_lucario` vs itself, seats
alternated, p = 0.5 by symmetry), unpaired — the engine exposes no seed on the Windows ship
ABI (`probe_cg_seed.py`), so `Cov = 0` is an engine fact, not an assumption.

| n/arm | single-arm SD | sigma_delta | sqrt(2)*SD | binomial SD |
|---|---|---|---|---|
| 400 | 0.02623 | **0.03529** | 0.03709 | 0.02500 |
| 1200 | 0.01725 | **0.02520** | 0.02439 | 0.01443 |
| 2400 | 0.01203 | **0.01626** | 0.01702 | 0.01021 |

The `sqrt(2)` match proves **Cov(arms) = 0** — independence, no cross-arm leak. It is
scale-free and does **not** prove binomiality.

### Operative bars — use these, never a binomial CI

```
n=400    95% CI half-width 6.92pp    MDE@80%  9.88pp
n=1200   95% CI half-width 4.94pp    MDE@80%  7.06pp
n=2400   95% CI half-width 3.19pp    MDE@80%  4.55pp
other n: sigma(n) = 2.520pp * sqrt(1200/n)
```

**Sizing standard: n>=1200 to admit or reject. n=400 is KILL-ONLY (supports only
">14pp worse"). Any null at n<800 must state its MDE.** Minimum n to detect the easiest
bronze-closing target (+14.81pp) at 80% power: **n≈180**; at n=300 the MDE was 14.11pp,
sitting *on* that target — those screens could not resolve it.

### Dispersion cause

```
lag-1 autocorrelation   -0.00159 +/- 0.00448   -> STATE LEAK RULED OUT (strong)
shuffle-slice, 200 perm  0.01437               -> marginal is Bernoulli(0.5)
fresh-worker, n=20       0.01789, 95% CI [0.01361, 0.02613]  -> binomial INSIDE
```

**Excess dispersion is UNPROVEN** — both observations have binomial inside their intervals.
The bars stand because `sigma_delta` was measured under our actual screening procedure and is
conservative either way.

---

## 3. Ladder physics — MEASURED

### Identical bytes do not converge

Enumerated by hashing `main.py` + `deck.csv` inside every tarball on Skynet (14 distinct
products), joined to the submissions ledger:

| family | n | legs | spread | sd |
|---|---|---|---|---|
| `739aae36` detox | 3 | 711.2 / 766.2 / 740.0 | 55.0 | 27.5 |
| `a81eab3e` V13 | 3 | 745.6 / 698.0 / 745.7* | 47.7 | 27.5 |
| `d8639a36` ours | 2 | 663.6 / 815.1 | *unusable* | — |

\* `55218076` still immature at T+75h. The third family is excluded: `815.1` froze at T+64h
(dethroned ~T+61.5h, 21h pre-maturity) and `663.6` was never polled — both non-observations.

**PLAN §12 assumption 8's convergence half does NOT hold.** Identical products settle ~50
apart and stay apart. **`sigma_draw` = 27.5 is a persistent band-entry term, not convergence
noise** — same number, different physics. Corroborated at ~27 from 4.6M episodes.

**The 50–400 band-width claim is UNVERIFIED.** Our two usable families measure 47.7 and 55.0;
we hold no identical-byte spread above 55 that survives a maturity check.

### Active-leg behaviour

`55218076` while ACTIVE: `743.8 -> 737.3 -> 741.6 -> 745.7` over ~17h — oscillation in an
~8mu band, not convergence to a point.

### Ship bar

```
break-even is an IDENTITY: candidate mu must beat V13's OWN family mean, and it is SIGMA-FREE
  (27.51/sqrt(pi) = 15.5209 vs banked 15.52; analytic + 400k-rep MC)
OPERATIVE BAR: ~722-739 mu as an INTERVAL. NO POINT VALUE.
Re-derive after 55218076 crosses T+85h at 2026-08-07T06:31Z, V13-family draws only,
excluding any leg frozen below 85h, naming sigma_between explicitly.
```

### Dates that bind

```
2026-08-07T06:31Z   55218076 reaches T+85h (maturation physics)
2026-08-16T23:59Z   submission wall (hard)
~2026-08-31         grading
2026-09-13          strategy report
```

Everything else is a derived ceiling, not a deadline.

---

## 4. Harness facts

```
--candidate-source forced --workers 1   eval.py:1447, :393. EVERY exact-product screen ran
                                        serial. The 151.75 g/s benchmark was measured on the
                                        MODULE-candidate path and never described them.
                                        Fix in PR #2691: pass the source path to the worker;
                                        load_agent_from_source allocates a fresh UUID tempdir
                                        per invocation. Serial 393-7-0 == parallel-8 393-7-0,
                                        exact-count parity. 250s -> 50s.
candidate_staging.py                    non-bundle branch copies main.py + deck.csv ONLY;
                                        bundle (config.py present) copies the whole tree.
```

---

## 5. Where we stand

```
our score        745.7 at T+75.7h (IMMATURE), 20-min poll cadence
rank             41st of 42 profiled competitors; 34 of 42 are >=1000mu
field leader     Majkel1337 1262.4mu, 72.1% real ladder WR over 384 games
candidates       8 screened 2026-08-06, all killed or null, ZERO cleared the bar
```
