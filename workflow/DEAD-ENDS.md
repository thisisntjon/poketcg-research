# DEAD-ENDS — what is closed, what it actually proves, and what would re-open it

**Authority:** JON-DIRECT 2026-07-28 (recovery discipline, item 4). Consolidated by Master.
**Status:** CURRENT. Additions require a measured basis; removals require a superseding ruling.
**Fences:** no games / no submissions / no spend / actuate=0.

> **READ THIS FIRST — THE PURPOSE OF THIS FILE IS TO STOP TWO OPPOSITE ERRORS.**
> A dead-end register that **understates** its negatives lets the fleet retread refuted ground.
> One that **overstates** them closes doors that were never actually shut. **The second failure is
> the more expensive one here, because it is invisible.** Every row therefore carries a
> **SCOPE GUARD**: the exact claim the evidence supports, and the claim it does NOT.
> **Cite the scope guard whenever you cite the row.**

---

## A. Product / science dead-ends

| # | Closed thing | Measured basis | SCOPE GUARD — what it does NOT prove | Re-opens if |
|---|---|---|---|---|
| A1 | **H + belief-PIMC search stack as a shipped product** | Ladder **476–554 μ**, below the borrowed community-1084 rules bot (~750); loses **20–0** same-deck head-to-head | Does **NOT** refute search as a **mechanism class**. Local superiority over H is still confirmed (E031/E033/E035/E062). It refutes *this stack, as shipped* | A search implementation with a non-weak evaluator is measured under an issued seq |
| A2 | **V13's dormant search, once mechanically alive** | seq-89/90: `561,117/561,117 search_begin` OK; then lost **24–0 to its own fallback** | **SK-294 SCOPE GUARD (JON-DIRECT):** may be cited **ONLY** as *this implementation, as enabled, loses to its own fallback*. **NEVER** as evidence that search is closed or refuted as a class. Both our negative datapoints were **weak-evaluator** implementations | Working-search is under active investigation; a non-weak evaluator changes the object |
| A3 | **Full-policy imitation on the 148-feature representation** | ~**0.744 AUC** wall | Does not close imitation on a *richer* representation | **AMENDED 2026-07-31 (conformance audit `#2106`):** a materially different representation reopens *this specific AUC-wall claim only*. It does **NOT** by itself reopen pre-wall BC generation firing — see **A13**, which gates that on a stronger teacher or an exceed-teacher mechanism, independent of representation quality. The original condition was a stale reopening path once A13's arithmetic bound; citing it alone would invite the exact re-entry the gate closed |
| A4 | **Family-1** — engine lethal/race scalars on 148 | S4 offline gate **BANK_NULL** (#1505, Δ≈+0.25pp) | A **particular test** that failed under a calendar/EV decision — **NOT** a universal disproof that representation can never work | — |
| A5 | **Family-2** — board card-identity scalars on 148 | S4 offline **BANK_NULL** (#1519, Δ≈+0.35pp ≪ +1.5pp bar, CI includes 0) | Same guard as A4. Two consecutive nulls on genuinely-new information **suggest** base-148 is near an observable-state ceiling — suggestive, not proven | — |
| A6 | **Cheap single-rule data-mined patches to the ~750 floor bot** | Diffuse null, n≥200 | Does not close *structural* rules work on the champion family | — |
| A7 | **Cross-archetype deck swaps / counter-decks as probe lanes** | The `54802103` class, live **μ 422**. Piloting incompatibility dominates any paper matchup edge | Does not close **within-family** list variants the champion rules can demonstrably pilot | A different archetype requires its own Jon-level ruling — it is a new product family, not a probe |
| A8 | **Representation re-architecture for this competition** | C5 disposition, killed permanently *for this competition* | Not a claim about representation in general | — |
| A9 | **Piloting setup-greed lane** | Closed **GATE_INVALID** | — | — |
| A10 | **Same-deck H2H as a qualified strength selector** | **2026-07-28, #1897:** `NO-QUALIFICATION-SET-AVAILABLE`. **0** fully-eligible positive controls, 1 partial, 2 unique pairs. 940 vs champion preserves strict ordering **0/2** — *a disagreement, not a missing receipt*. V13 ≈0.55 against a materially higher anchor | Does **NOT** prove H2H is invalid. It proves the **evidence floor is currently unreachable**. **No compute may be spent on an H2H calibration run** — a precise answer from a disqualified judge is worse than none | **1 fully-eligible positive control** (resolved identity + immutable bundle + mature anchor joined to the exact deck identity) **PLUS** a second independent case. Missing cells enumerated in #1897 |
| A11 | **The owned value-net as an action selector** | G1: `net_top1_is_recorded` **0.345** vs champion **0.587** on recorded elite actions | ONE metric on ONE checkpoint. Does not condemn the value net generally. **SK-332/SK-354:** agreement-with-champion is **ceilinged at reproducing the champion** and can never demonstrate improvement *on* it | **WO-STRAT-02 value-leaf fence was UN-HELD by JON-DIRECT 2026-07-28 for that lane only** (seq-113). All other §0a doors stand |
| A12 | **`_SEARCH_OK` as evidence of free headroom** | Clean bundle extraction imports the four search symbols; Linux binaries expose them; engagement stayed vacuous in-harness; the repaired search-enabled derivative then lost 24–0 | "Fallback-only means free upside" is **contradicted**. Live Kaggle engagement remains **unobserved** — a zero-game fidelity check still matters for *teacher identity*, not as a presumed strength unlock | — |
| A13 | **BC generations reaching bronze pre-wall** (added 2026-07-31, conformance audit `#2106` — this row was **missing entirely** before the audit, on the file whose exact purpose is preventing that) | `workflow/PLAN-LEARNING-LOOP.md` criterion 8, quoted verbatim: *"G1 verified our teacher's μ is genuinely 711-753 against a bronze cutoff of 840.4 (falling, extrapolated ~805-815 by the wall). Every affordable objective's fixed point is at or below its teacher... even a perfect learned parity net lands ~753 against a ~805-815 wall."* Corroborated by **6 learned candidates across 2 generations, zero survivors** (gen-1: 4 cells, gen-2: 2 cells) | **SUPPORTS:** no pre-wall BC generation fires without a named exceed-teacher mechanism (stronger teacher or search-on-top). **DOES NOT SUPPORT:** label work or representation research being dead — re-filed to **Sept-13**, not killed. **RE-STAMPED 2026-08-23 (probe C-L; this cell predated 08-18 and still said "DAgger authoring ... re-filed to Sept-13, not killed"):** the DAgger line on the SAME teacher is now a **measured NULL** (2026-08-18: 5 cycles, 14/800, overlaps teacher) and the prereg **forbids cycle-6 on the same teacher** (ATTEMPTS-LEDGER §1 fact 4, §7 Forbidden); DAgger reopens only with a different/stronger teacher or a named exceed-teacher mechanism. The 6 failures share ONE apparatus (148-feature representation, one corpus family), so the corroboration is narrower than the count suggests — **the ARITHMETIC carries the ruling, not the 6**. This is a scarcity decision for the pre-wall window ("we will not spend on X pre-wall"), never a scientific claim that BC/imitation cannot work ("X does not work") — the ledger's own corrected language, after Master repeated that exact overclaim once and withdrew it | **A teacher ≥ the wall, or a named non-imitation/exceed-teacher mechanism** (`PLAN-LEARNING-LOOP.md` elimination ledger, tier ARITHMETIC-CLOSED) |
| A14 | **The B339 RLVR value net as the leaf of the shipped determinized searcher** (added 2026-09-01, M1 / Gate 1(a) terminal) | **M1, both arms n=1,200 vs exact public c61, process-isolated one-process-per-game, zero faults.** ON (leaf) **6.08%** Wilson [5.04, 7.32] · OFF (hand leaf) **13.50%** [11.96, 15.20] · ON−OFF **−7.42pp** 95% CI [−9.78, −5.06], pooled z −6.113, Fisher exact p 1.00e−09, odds ratio 0.415. Frozen kill (ON Wilson CI-upper < 0.50) fired at 0.0732 → **RETIRE**. The leaf swapped in has **higher** held-out AUC (0.774) than the one it replaced (0.610) | **SK-294 SCOPE GUARD.** Cite ONLY as: *this leaf, in this determinized searcher, at this wiring, plays materially worse than the hand leaf it replaced.* It does **NOT** close value-guided search as a **class**, it does **NOT** show learned leaves cannot work, and it is **NOT** a licence for "AUC is worthless" — it supports the narrower statement the P10 pre-registration named in advance: *leaf ranking accuracy is not the quantity PIMC needs here.* **Three instrument explanations were tested by a non-author and REFUTED before this row was written** (SKYNET `5501405179`): sign inversion (`value_net.value_pm1 = 2*p_win − 1`, a correct [−1,+1] map); scale mismatch (terminals ±1.0, net ±1, `view.evaluate` "roughly [−1,1] via tanh squash" — same scale); and seat rooting (the root-relative negation is byte-identical in the net branch and the heuristic branch). So this is a mechanism result, not an artifact — **and that is exactly why the scope guard matters more, not less** | A materially different **wiring** of a learned leaf (different aggregation, different quantity at the leaf, or a leaf consulted somewhere other than `_leaf_value`), or the same leaf inside a **different searcher**. Note also the OFF arm's own number: `POLICY=search` is **13.50%** vs `rules_lucario`'s **43.14%** (n=12,000) — anything built on this searcher starts 29.64pp behind the product we already own, and that arithmetic, not this row, is what should govern spend on the lane |

---

## 🔴 RECLASSIFICATION PASS — 2026-07-31 (Master). READ BEFORE CITING ANY ROW BELOW.

**Two defects were verified today, and every row in this file predates both.** Until this pass, the
register still read as though neither existed — the exact delivery-gap it exists to prevent.

1. **The band pool measured SELF-SIMILARITY.** Opponents were real field decklists piloted by *our
   own generic heuristic*. Our lineage scored 97–99% against it; a mechanically independent agent
   scored 82–92% *and beat both our kernels head-to-head*.
2. **The ship path never carried our logic.** The submitted bundle is `main.py + deck.csv + cg`, and
   that `main.py` reads **no config, no env, no flag**. Every `rules_lucario.py` knob experiment ran
   on a path that could not reach the ladder. (`rules_lucario.py` is also a *different lineage* from
   the fielded kernel.)
3. **And the dominant defect turns out to be neither — it is POWER.** MDE at n=400/arm is **9.84pp**
   against a protocol requiring n≥1200/arm for 5–8pp claims. Most "nulls" are 3–6pp effects read on
   an instrument that resolves 10pp.

**A row is VOID if its basis dies to (1) or (2). It is UNKNOWN if it dies to (3) — underpowered is
not refuted. It is SOUND if it was measured on the LIVE LADDER, on the SHIP PATH, against REAL
FOREIGN BYTES, or purely OFFLINE with no opponent pool.**

**Most of this file survives.** That is the finding, and it is why this is an annotation and not a
demolition — a register that overstates its negatives is the more expensive failure, and so is one
that panics.

| row | verdict | reason |
|---|---|---|
| **A1** search stack as shipped | **SOUND** | measured on the **live ladder** (476–554 μ) + 20–0 same-deck. Both defects inapplicable |
| **A2** V13 dormant search | **SOUND** | measured **on the ship path**; lost 24–0 to its own fallback |
| **A3** 148-feature imitation AUC wall | **SOUND (apparatus-scoped)** | offline, no opponent pool. ⚠ see the A5 tension below |
| **A4** Family-1 scalars | **SOUND (apparatus-scoped)** | offline S4 gate |
| **A5** Family-2 board card-identity scalars | **SOUND, WITH A LIVE TENSION** | offline — but a 2026-07-31 probe reports **card-identity EMBEDDINGS broke the feature ceiling** while hand-visibility bought only **+0.007 AUC**. Scalars-on-148 and identity embeddings may not be the same object. **Do not cite A5 against the identity lane without resolving that.** |
| **🔴 A6** cheap single-rule patches to the floor bot | **🔴 VOID — DOES NOT SURVIVE** | basis is *"diffuse null, n≥200"*: opponent set **NOT LOCATED**, almost certainly the harness-flag path (defect 2), and n≥200 is **far under** the n≥1200 the effect size needs (defect 3). **This row closed the exact lane we are now building in.** Returns to **UNKNOWN** — untested, not refuted |
| **A7** cross-archetype deck swap | **SOUND** | measured **on the ladder** (μ 422) and actually shipped. Keep the narrow scope guard |
| **A9** piloting setup-greed | **UNKNOWN** | closed `GATE_INVALID` with **no measured basis stated**. A gate-invalidity is a procedural outcome, not a refutation |
| **A10** same-deck H2H as a qualified selector | **SOUND, AND REINFORCED** | an evidence-floor finding, not a strength claim. Reinforced today: a **free, zero-game null control already exists** (940 and V13 are bytecode-identical, 240/240 bit-identical) and discharges half its reopening condition |
| **A11** value-net as action selector | **SOUND, WITH ITS OWN GUARD** | its SK-332/354 guard — agreement-with-champion is *ceilinged at reproducing the champion* — is the same defect class as (1), one level up, and is already stated on the row |
| **A12** `_SEARCH_OK` as free headroom | **SOUND** | the contradiction is mechanical |
| **A13** BC generations pre-wall | **SOUND** | **the ARITHMETIC carries it, not the 6 cells** — teacher μ 711–753 against a wall of 805–815. Re-measuring the 6 on a better pool does not change subtraction |

### 🔴 The consequence that matters most

**A6 closed "cheap single-rule patches to the ~750 floor bot" — and that is precisely the lane the
current plan builds in** (Tier-0 correctness fixes, Tier-1 HP-aware self-preservation). We were
about to build inside a lane our own register calls closed, on **the single weakest row in the
file**.

**It is now VOID, so the lane is open — but state the distinction precisely rather than using this
as a blanket permit:**

- **Tier-0 (negative-score filter, `try/except`, `benchMax`) was NEVER inside A6's scope at all.**
  A6 covers *data-mined rule patches*. Tier-0 items are **defect removals** — we currently take
  options our own scorer set to `-1`, and we ship no exception handler. Fixing a bug is not a
  strength bet and needs no dead-end row lifted.
- **Tier-1 (HP-awareness) WAS inside A6's scope, and is now released** — on the grounds that A6's
  basis does not survive, not on the grounds that we would like it to.

### What this pass does NOT license

Nothing here reopens **A1/A2** (search), **A13** (pre-wall BC), or **A7** (cross-archetype decks).
Those rows were measured on the ladder, on the ship path, or by arithmetic — the two defects do not
touch them, and **the re-audit is selective for exactly this reason.** Anyone citing "the record is
invalid" as a general permit is misreading this pass.

**Owed next:** the same pass over `GATES.md` and `IMPROVEMENT-INVENTORY.md`, whose rows carry the
same two defects and are not yet annotated.

---

## B. Retired dates and derivations

| # | Retired | Why |
|---|---|---|
| B1 | `D1` Aug-13 "last-mature-submit", `D2` Aug-12, `D3` ~Aug-9, `D4` ~Aug-5 "probe ceiling" | **No such requirement exists.** Post-close convergence (Aug-17â†’~31) grades anything submitted by the wall. `D4` is the date wrongly propagated as a schedule |
| B2 | "Experimental-probe cutoff 2026-08-05" | Derived from the wrong `D1`. **Not a real wall** |
| B3 | "T+72 is a mature read" | T+72 is a **grade**. Maturity is T+85–100; pre-registered mature read `2026-07-31T18:01Z` |

**The only REAL walls:** `2026-08-09 23:59` entry/merger · **`2026-08-16 23:59` final submission** · `2026-08-17→~31` convergence · `2026-09-13 23:59` Track-2. **Dates are CEILINGS, never schedules. Not-submitting-while-ready is the named forbidden failure.**

## C. Dead instruments (2-strike register)

**Rule (JON-DIRECT 2026-07-28): an instrument that fails twice the same way is DEAD for the session.
Post it and route around. No silent retries.**

| # | Instrument | Strike 1 | Strike 2 | Status |
|---|---|---|---|---|
| C1 | **Master's "silent seat ⇒ dark ⇒ reassign" inference** | Misty declared dark at 60m; posted a VERIFY PASS **71 seconds** after Master reassigned her object | 5080 declared dark and W2 transferred to Roach; 5080 delivered W2 as **#1898** minutes later | **DEAD for this session.** Root cause: **deep work has no observable in-progress signature.** A verifier produces nothing until the verdict; an author pushes nothing until the PR. Silence from a thinking seat is **not weak evidence of death — it is no evidence at all**. Route around a silent seat by giving *other* seats work; **do not reassign the silent seat's object** |
| C2 | `days_since_last_submit` (MASTER_DASHBOARD) | Reports 7.42 / `last_submit_utc 2026-07-21` while its own Latest-2 block lists `55034300` at age 1.01d | — | Owned by Brock (A-brock-O1-DASHBOARD). **Not the ONE METRIC** |
| C3 | Master's seat-liveness alarm (**repaired**) | Associated by **substring**: a post *about* `5080` counted as a post *by* it, so the alarm could never fire | — | Repaired to leading-self-tag + `status/<seat>` commit clock; control-tested both directions |
| C4 | Hub worktree copies of tracked files | `master_dashboard.py` local mutant `152+/575âˆ’`; `INBOX-brock.md` 113 lines short | Recurs continuously | **Never read repo state from the hub worktree.** `git show origin/main:<path>` only |

## D. NOT dead — do not add these by mistake

- **The ONE METRIC** `days_since_last_ladder_mu_experiment` is **CORRECT**. It measures replicated candidate **PAIRS**. `STARVED` is **true**: no candidate pair has shipped in 7.4 days. Master called it wrong once and **retracted**.
- **Measurement of search internals** is **OPEN** under an issued seq (SK-293). Only **shipping** is closed.
- **Within-family list variants** the champion rules can pilot remain admissible probe lanes.
- **The offline strength selector** is an **unfilled role**, not a dead end. A10 closed a *candidate* for it; it did not fill it.

## E. Killed 2026-08-06 (Master, from the F0b census)

| id | Idea | Why it is dead | Evidence |
|---|---|---|---|
| E1 | ~~**WIN-01 "fix `AdvancedPolicy.choose()` maxCount truncation so MCTS fires"**~~ **RETRACTED 2026-08-06 by Master (own error).** | **The original kill reasoning was CIRCULAR and is withdrawn.** It read: *"fixing it cannot help: `search_begin` is never attempted at all, so the candidate list never reaches a search that could use it."* That uses the CONSEQUENCE of the bug as the reason the FIX would fail. Ash's own diagnosis states the causal direction plainly: `choose()` truncates to maxCount=1, **causing** `SEARCH_ALGO` to see one candidate and return without calling `search_begin()`. Remove the truncation and `search_begin` fires. The layer IS reachable. The truncation-fix question is REOPENED and is being measured under `MASTER-SEQ-0806-SEARCH-ACTIVATION` (bounded, zero-ladder, per SK-293; SK-294 confirms search is not refuted as a MECHANISM CLASS — only this-implementation-as-enabled). **E2 is unaffected and STANDS: the "+0.84 causal delta" half of WIN-01 remains dead, with no artifact in git.** Lesson: a kill is a claim and needs the same causal scrutiny as a promotion. This one was written under time pressure by the seat that is supposed to catch exactly this. | Ash F0b census, PR #2644, on exact fielded V13 archive sha256 `353e0530a345fe6ab94b3d8fa2c8f14b9d356de538c9c52010d71e1dcf54952e`: `_SEARCH_OK=True`, `USE_SEARCH=True`, `search_begin_attempts=0` across 3,195 agent invocations / 1,844 main-context prompts. Consumer verification by Brock pending on head `307b59660`. |
| E2 | **"+0.84 causal delta for Suboptimal Bench Switching"** | **No artifact exists in git.** The figure was asserted in a Master dispatch ([MASTER-ROACH-F2-RESUME]) and then used to justify fitting evaluator constants and compiling WIN-01. F2 never ran. Retracted 2026-08-06. | Searched all of `origin/main`: no receipt, no JSON, no committed measurement. Roach never acknowledged the dispatch. |

**The lesson, and it is the expensive one:** both halves of our only named candidate were
hollow — one built on a number nobody measured, one aimed at code that never executes — and
the funnel ordering (F0 causal label -> F2 -> F3 compile) is exactly what would have caught
it before compute was spent. The ordering was inverted under ship pressure. Registry
assumption 10 ("ship-pressure degrades receipt integrity") is CONFIRMED again.
