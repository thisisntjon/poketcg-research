# Cold-start acceptance test — 2026-09-05 (SRI Phase 8)

Rule: answer using ONLY `INDEX.md` plus at most two register opens. A question that cannot be answered is a hole in the inventory, recorded here with its fix, never papered over. Author of the layer = SKYNET; this first run is the author's own pass and does not count as the independent test (that is Subject 3 / MISTY in `workflow/research/2026-09-05-archive-cleanup/ONBOARDING-TEST-PROTOCOL.md`).

| # | Question a resuming agent must answer | Answered from | Answer | Result |
|---|---|---|---|---|
| 1 | What is the current source of truth for which bytes were submitted and which bytes are the experimental Parent? | INDEX §five things #5 | `workflow/inventories/IDENTITY.json`, rendered from git; the two differ (repo `main.py` ee86a5de vs built asset 12f2aeba), and which Parent is of record is an open question to ASTRA (INVENTORY A-row next_action). | PASS |
| 2 | What did we already try for "cheap single-rule patches on the floor bot" and why did it fail? | INDEX → `registers/LEARNINGS.md` (rows L-0006, L-0125, L-0135, L-0367, L-0401) | Tried as DEAD-ENDS A6 / graveyard G09; the original "diffuse null, n≥200" basis was VOIDED on 2026-07-31 (opponent set not located, harness-flag path, n≥200 far under the n≥1200 the effect needs). Status is UNKNOWN — untested, not refuted; class FALSE_NULL; reuse = retest on the ship path. | PASS |
| 3 | Which numbers must never appear in the write-up? | INDEX → `registers/CLAIMS.md` (status RETRACTED, c-036…c-047) | 753.1, 815.1, 151.5, 729.10, 743.8, 2.199, 46.2/46.21, 0.0219/88.7/82.90/93.05, the two hashes b10dcd7ba722 and ba51cc113af4, +9.35pp, +8.26pp — each row carries "use instead". | PASS |
| 4 | How many games does a screen need before it may admit a candidate, and what may n=400 do? | INDEX → `registers/METHODS.md` (M-002, M-004) | **Corrected 2026-09-06 (ASTRA, method-recovery audit):** the answer first written here repeated the historical "n≥1200 to admit or reject; n=400 KILL-ONLY" rule as if it were a universal inference rule. It is an operational policy: a CI such as [−1, +1] pp has a negative lower bound without establishing harm; operational stopping and scientific refutation differ. What the sources support: MDE₈₀ at n=400 is 9.88 pp and at n=1,200 is 7.06 pp under the measured σ_Δ (`MEASURED-2026-08-06.md`), so a screen at n=400 cannot resolve a 5–8 pp effect; the historical policy chose to treat it as kill-only. Per-arm SE at p=0.5 is 100·√(0.5/N) pp (100/√T for total T = 2N). This answer must not be used as the independent answer key unchanged. | PASS (corrected) |
| 5 | Is search closed as a mechanism class? | INDEX §Do NOT resume + LEARNINGS A1/A2 scope guards | No. What is closed is *this stack, as shipped* (A1, ladder 476–554 μ, 20–0 same-deck) and *V13's search as enabled* (A2, 24–0 to its own fallback, SK-294). Both negatives were weak-evaluator implementations; a non-weak evaluator reopens the question. | PASS |
| 6 | What was the last experiment, why did it park, and what named condition would reopen it? | INDEX §Current state → `registers/OPEN.md`, `registers/LEARNINGS.md` | INDEX names it (ASTRA's 40-game empty-bench screen, zero reach, parked by its pre-registered rule) but **no register row carries the reopen condition** ("a decision family observed in Parent's actual play, the MAIN family, not the frozen predicate"). The receipt `ptcg-agent/experiments/analysis/2026-09-05-astra-empty-bench-cycle.json` exists only on `origin/codex/astra-context-roadmap-review`, not on main, so no harvester can see it. | **HOLE** |

**Result: 5/6.** The hole is a banking defect, not a generator defect: an experiment whose receipt is not on main does not exist to the inventory (ATTEMPTS-LEDGER §8a banking rule; Jon 2026-09-04 "work not in the inventory does not exist"). Fix owned by ASTRA: land the receipt on main (its own PR), add the row to `TRIED-RESULTS-COMPACT.json` with `src`, and the next `build_sri.py` run harvests it. Until then the packet `workflow/inventories/packs/player-connection.md` is the only surface that answers Q6, and it reads the branch directly.

## Measured on this run
- INDEX.md 60 lines (cap 150); the registers total ≈ 770 KB and are opened on demand, never loaded.
- Gate: PASS — 2,383 manifest records, 0 fails, 0 warnings (validate.py G1–G7, claims gate on 3 verification-asserting rows).
- Claims: 47 rows — 12 RETRACTED, 3 VERIFIED-AT-ARTIFACT, 4 UNVERIFIABLE-HERE (off-repo producers), 28 RECEIPT-WEAK (NUMBERS-INVENTORY rows whose producer is named by bare filename or command, not a repo path — the next harvest pass resolves those producers or marks them BLIND).
- Learnings: 425 rows (dead-end 35, ledger 317, tried-result 32, graveyard 30, instruments-that-lied 8, kill-card 3), every row with `path L<line> (blob …)`.
- Decisions: 1,739 of the 1,741 pipe rows in DECISIONS.md (2 are the column-name row and a separator).
- Tools: 494 scripts (tests excluded); 490 test/conftest files under the same roots are censused by curate and not repeated (ASTRA #3458 finding: file count is not capability).
- Source defect found, not fixed here: `workflow/DEAD-ENDS.md` carries mojibake (`â€”`, `nâ‰¥200`) in several A-rows — the file was saved through a cp1252 round-trip at some point. The registers inherit it faithfully; the fix is a byte-level repair of the source on its own PR with `git diff --stat` proof that only those bytes changed.
