# LESSONS — the human-readable index above the M-rules

**Status: ADVISORY v1 (advisor-authored from the 2026-07-25 record; [verify-me: Master]).**
One row per lesson: the story in plain language, the root cause, the mechanism it became, and
receipts. The M-rules and CI gates ENFORCE these lessons; this file makes them FINDABLE.
Format rule: new lessons append with receipts in the same commit (REPORT-ANCHORS Rule 1).

| # | lesson (plain language) | root cause | became mechanism | receipts |
|---|---|---|---|---|
| 1 | A guard that measures through the thing it guards goes blind exactly when needed — ram_watch spawned PowerShell to measure a PowerShell-spawn storm | instrument shares the failure mode it detects | in-process ctypes probes; cross-box watcher on an unaffected box | SK-62/SK-78; commit-charge memory |
| 2 | A green fence can pass a bundle that plays uniformly at random — fences checked DECLARATIONS, not executability | unknown-POLICY fell through to choose_random silently, recorded nothing | seq-63 fail-loud terminal fallback; seq-64 ship-path executability (execute one real decision, observe a non-default action) | SK-88; 71e1fa623; 6c221b27d |
| 3 | Six variants of ONE defect class: "the agent = the champion" was baked into every loader/driver/reference for three weeks, because a single-agent codebase never parameterizes identity | un-varied assumptions never became variables | identity census (37 sites, 4 reading effective identity, 0 with a firing refusal test); identity_gate + --reference-first as harness code; per-site constructed refusal tests | SK-117/#1725; #1730; #1729/86ca02d06 |
| 4 | A 420-game screen reported all-clean while running the WRONG AGENT — only the mandatory zero-fallback counter-attestation exposed it | eval loader copied partial bundles; instrument had no identity check | counter-attestation mandatory on every screen (fire-proof doctrine, generalized); loader copies FULL bundles | instance five; #1724/af6c04dfd |
| 5 | A certificate could report SOAK_PASS having measured NOTHING — every counter vacuously clean on an empty input | no n>0 floor; vacuous pass | M14: no certificate without positive measurement; construct the input that makes each guard FAIL — a check that cannot fail is not a check | Misty verdict 06:47Z; SK-91; 3de3048cd |
| 6 | Claims about objects went stale invisibly — five "you owe me / no you owe me" deadlocks in one night, every claim TRUE about a state that had already changed | staleness looks identical to freshness in prose | M1: claims carry the object's sha or are void; M5 mutual-block detector; M3 one-surface-per-question | TEAM-PROTOCOL v2; 77eb9273a |
| 7 | Session errors are context-length physics, not model failure or carelessness — cross-model comparison made it a near-clean experiment | long-context state-heavy work degrades identically across models | M17 tools-not-resolve; M16/M18 dynamic wakes + delta orientation; verify_kit/seat_pulse replace hand-runs | SK-99; 6b3af9660 |
| 8 | Two-dot diffs "show" merges deleting things merges do not delete — a false stale-base theory blocked a clean PR | wrong diff semantics (main..branch vs main...branch) | M10: three-dot only; SK-85 retraction with measured table | SK-85; 2f2c85a32 |
| 9 | Governance docs shipped with defects code review would catch — a missing owner field reddened fleet CI; a date registry retired dates its own dashboard still used | governance edited without validation | M12: validate governance like code; retirement-propagation rule (retiring a date requires same-cycle grep) | DATES.md; SK-71 era |
| 10 | Internal dates quietly became walls and nearly cost experiments — "not-submitting-while-ready" named as the forbidden failure | derived dates read as REAL by agents that take dates seriously | DATES.md registry: [REAL]/[DERIVED]/[RETIRED] labels; dates are CEILINGS, never schedules; unlabeled dates bind nothing | 9ec5e8e60; DATES.md |
| 11 | The competition's own physics were assumed, not measured — the schedule INVERTED on contact with the primary source (ratings mature ~2 weeks past close) | derived schedule built on an unverified anchor | anchor semantics verified from source before derivation; read budget corrected UPWARD | SK-72/SK-102 era; Jon page-read |
| 12 | The scoring rule was worth ~+17-28u and nobody had checked it — team score = MAX(latest-2), measured from our own floor pair | mechanics assumed instead of measured | SK-80 ruling: duplication strictly increases P(>=bronze); endgame Error-runbook (riskier first, tested last) | SK-80; advisor 04:01Z |
| 13 | Our sim cannot RANK candidates vs the ladder (rho=-0.50 measured, confounded) — local strength claims are structurally untrustworthy | representation/population gap between sim and ladder | ladder-first doctrine: sim = brokenness screen ONLY; every selection rests on a live read; reads are the budget currency | seq-52; #1705 |
| 14 | Pre-registration converts disputes into arithmetic — kill bars, materiality bands, branch tables, acceptance criteria, even packet FIELDS frozen before numbers exist | post-hoc judgment bends toward the result | freeze-before-measure at every layer; "a prediction that can be embarrassed is the point" | SK-65/78/100/111; Misty 12:38Z |
| 15 | The verifier seat pays for itself in hours — a fresh dedicated verifier failed the safety instrument on its first claim and every verdict got faster | verification was the throughput governor and a single point of failure | Misty seat (verify-only charter); author XOR verifier absolute; M8 verifier-down protocol | SK-90/91; Misty launch 06:41Z |
| 16 | Estimates need mechanisms too — soak ETA missed 2x because an instrument's overhead was unpriced; "report PROGRESS, not CPU" | estimates asserted without decomposition | SK-129 progress-not-CPU ruling; M21 progress/checkpoint as CI GATE (which caught its own false-pass) | SK-129/130 |
| 17 | Copying is the field's rational strategy WITHOUT a factory — the frontier froze into a monoculture because unverified modification is treacherous; verified modification is our open lane | modification risk without verification infrastructure | the factory itself: 22 PRs verified+merged in one day with zero defects reaching the ladder | census #1708/#1730; cycle-1 metrics |
| 18 | The live product must be attested from production behavior, not local proof — and the attestation's own first run had an instrument bug its falsifier caught | instruments are guilty until their falsifier fires correctly | seq-78 behavioral attestation (a_c=1.0000, 4,022/4,022); falsifier-fired-then-isolated-to-instrument pattern | #1748; SK-153/155 |
| 19 | Platform mechanics must be resolved from RECEIPTS, not from either rules boilerplate or forum posts alone — the two contradicted each other and only an authenticated UI check settled it | multiple authoritative-looking sources, one operative mechanism | finals = latest-2 CLOSED in DATES.md on a UI receipt (no selection control exists) | SK-156; Jon browser check 7-26 |
| 20 | A verifier must not repair the gate blocking its own passed artifact — route the gate fix to a different owner | self-interested gate-loosening is invisible to the actor | Misty's bank_lint recusal (diagnosed both false positives, declined to fix, routed) | Misty 01:18Z 7-26 |
| 21 | A dormant guard is worse than no guard, because its silence is read as a pass — `test_gate1_action_terminal` had no `__main__`, so it collected nothing and exited 0 for weeks; the night it was given an entry point its FIRST act was refusing an unauthorised mutation of a frozen Gate-1 identity | a suite is credited on its exit code, and a suite that collects nothing exits 0 | `ci_discover_suites` fails closed on test carriers with no entry point; 25 suites repaired, ~441 cases now execute (161→169 RUN set) | #3135; SKYNET `5498976665`/`5499137224` |
| 22 | A control that exists and is never joined lets two seats reason their way to opposite answers about the same number — the intrinsic-seat mirror (I-6′, 9,604 games) had been banked 90 minutes before SKYNET and STRATEGIST each guessed at the c61 seat gap | banked evidence is not searched before a claim is reasoned about | grep the banked corpora for an existing control BEFORE proposing a measurement; it produced intrinsic +2.06pp / matchup residual +5.46pp [+2.05,+8.87] at zero new games | I-4c `5503718386`; I-6′ `5503307738`; SKYNET `5503819885` |
| 23 | A point estimate inside a useless interval is not a direction — an n=128 mirror gave +6.25pp (CI [−11.0,+23.5]) and was reported as arguing "close to a dead end"; the powered read was the opposite | the interval was stated and then the point estimate was still allowed to carry a conclusion | when CI spans the decision boundary, report NO direction; say only what n would resolve it | SKYNET `5503791988` (wrong) → `5503819885` (corrected) |

## Standing capture rule
A lesson enters this file when its mechanism lands, in the same commit where practical. Story
first, receipts always. Prune nothing — supersede with a pointer.
